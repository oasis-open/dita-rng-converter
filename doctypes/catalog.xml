<?xml version="1.0" encoding="UTF-8"?>
<catalog xmlns="urn:oasis:names:tc:entity:xmlns:xml:catalog">
   <nextCatalog catalog="rng/catalog.xml"/>
  <!-- NOTE: Putting the 1.3 grammars first so that version-independent
             URNs will resolve to the 1.3 versions.
    -->
   <nextCatalog catalog="oasis/1.3/rng/catalog.xml"/>
   <nextCatalog catalog="oasis/1.2/rng/catalog.xml"/>
</catalog>
