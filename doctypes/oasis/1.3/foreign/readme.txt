This directory contains the source DTDs and XSDs for foreign
vocabularies. These provide the master copies that are then
copied into the appropriate place when generating XSDs and
DTDs. The RNG versions of these foreign vocabs are held
with the RNG source since they can be directly referenced
from the RNG source.